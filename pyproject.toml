[project]
name = "remove-bg"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.7",
    "pillow>=12.0.0",
    "rembg[cpu]>=2.0.67",
]
